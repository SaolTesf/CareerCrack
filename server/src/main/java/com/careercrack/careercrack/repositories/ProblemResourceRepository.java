package com.careercrack.careercrack.repositories;

import com.careercrack.careercrack.models.ProblemResource;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProblemResourceRepository extends JpaRepository<ProblemResource, Long> {
}
